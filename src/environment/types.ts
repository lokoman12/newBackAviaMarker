enum Environment {
  Development = 'dev',
  Production = 'prod',
}

export enum DATABASE_TYPE {
  mysql = 'mysql',
  postgresql = 'postgresql',
}

export enum Airports {
  ULLI = 'ULLI',
  UUEE = 'UUEE'
}

export type Position = Array<number>;
